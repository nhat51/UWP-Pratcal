<Page
    x:Class="Pratical.Pages.EmployeeList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pratical.Pages"
    xmlns:model="using:Pratical.models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ListView x:Name="List_Employee" Loaded="List_Employee_Loaded">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="model:EmployeeModel">
                <StackPanel>
                    <TextBlock x:Name="{x:Bind name}"/>
                    <TextBlock x:Name="{x:Bind role}"/>
                    <TextBlock x:Name="{x:Bind birthyear}"/>
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</Page>
